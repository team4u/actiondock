---
phase: "1"
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - scriptflow-core/src/main/java/org/team4u/scriptflow/domain/port/ScriptRepository.java
  - scriptflow-core/src/main/java/org/team4u/scriptflow/domain/port/ExecutionRepository.java
  - scriptflow-core/src/main/java/org/team4u/scriptflow/domain/port/ScriptEngine.java
  - scriptflow-core/src/main/java/org/team4u/scriptflow/domain/port/JsonCodec.java
  - scriptflow-core/src/main/java/org/team4u/scriptflow/domain/port/PluginRegistryRepository.java
autonomous: true
requirements:
  - COMMENT-03
must_haves:
  truths:
    - "Port interface classes have Javadoc explaining their purpose and design intent"
    - "Methods have Javadoc with @param, @return tags"
    - "Interfaces are clearly documented as abstractions for infrastructure implementations"
  artifacts:
    - path: "scriptflow-core/src/main/java/org/team4u/scriptflow/domain/port/ScriptRepository.java"
      provides: "Script persistence abstraction"
      min_lines: 5
    - path: "scriptflow-core/src/main/java/org/team4u/scriptflow/domain/port/ExecutionRepository.java"
      provides: "Execution record persistence abstraction"
      min_lines: 5
    - path: "scriptflow-core/src/main/java/org/team4u/scriptflow/domain/port/ScriptEngine.java"
      provides: "Script execution engine abstraction"
      min_lines: 5
    - path: "scriptflow-core/src/main/java/org/team4u/scriptflow/domain/port/JsonCodec.java"
      provides: "JSON serialization abstraction"
      min_lines: 5
    - path: "scriptflow-core/src/main/java/org/team4u/scriptflow/domain/port/PluginRegistryRepository.java"
      provides: "Plugin registry persistence abstraction"
      min_lines: 5
  key_links:
    - from: "ScriptApplicationService"
      to: "ScriptRepository"
      via: "dependency injection"
    - from: "ExecutionApplicationService"
      to: "ExecutionRepository"
      via: "dependency injection"
    - from: "ScriptApplicationService"
      to: "ScriptEngine"
      via: "dependency injection"
---

<objective>
为 scriptflow-core 模块的 Port 层添加符合 Java 规范的专业 Javadoc 注释。

包含 5 个接口：ScriptRepository, ExecutionRepository, ScriptEngine, JsonCodec, PluginRegistryRepository。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@scriptflow-core/src/main/java/org/team4u/scriptflow/domain/model/ScriptDefinition.java
@scriptflow-core/src/main/java/org/team4u/scriptflow/domain/port/ScriptRepository.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Javadoc to Repository interfaces</name>
  <files>
    scriptflow-core/src/main/java/org/team4u/scriptflow/domain/port/ScriptRepository.java
    scriptflow-core/src/main/java/org/team4u/scriptflow/domain/port/ExecutionRepository.java
    scriptflow-core/src/main/java/org/team4u/scriptflow/domain/port/PluginRegistryRepository.java
  </files>
  <action>
使用 Edit 工具为以下 3 个 Repository 接口添加 Javadoc：

**1. ScriptRepository.java**:
```java
/**
 * 脚本仓储接口，定义脚本的持久化操作。
 * <p>
 * 该接口是领域驱动设计中的 Port，用于解耦领域模型与基础设施实现。
 * 实现类负责将 ScriptDefinition 持久化到具体的存储介质（如数据库）。
 *
 * @author jay.wu
 */
```
方法 Javadoc:
- `save(ScriptDefinition definition)` - `@param definition 脚本定义` `@return 保存后的脚本定义`
- `findById(String id)` - `@param id 脚本ID` `@return 脚本定义，不存在则返回空 Optional`
- `findAll()` - `@return 所有脚本定义列表`
- `deleteById(String id)` - `@param id 脚本ID`

**2. ExecutionRepository.java**:
```java
/**
 * 执行记录仓储接口，定义执行历史的持久化操作。
 * <p>
 * 负责 ExecutionRecord 的持久化和查询，支持按脚本ID筛选。
 *
 * @author jay.wu
 */
```
方法 Javadoc:
- `save(ExecutionRecord record)` - `@param record 执行记录` `@return 保存后的记录`
- `findById(String id)` - `@param id 记录ID` `@return 执行记录，不存在则返回空 Optional`
- `findByScriptId(String scriptId)` - `@param scriptId 脚本ID` `@return 该脚本的所有执行记录`
- `findAll()` - `@return 所有执行记录`
- `deleteById(String id)` - `@param id 记录ID`
- `deleteByScriptId(String scriptId)` - `@param scriptId 脚本ID`

**3. PluginRegistryRepository.java**:
```java
/**
 * 插件注册仓储接口，定义插件信息的持久化操作。
 * <p>
 * 负责 PluginRegistration 的持久化，支持按插件ID和启用状态查询。
 *
 * @author jay.wu
 */
```
方法 Javadoc:
- `save(PluginRegistration registration)` - `@param registration 插件注册信息` `@return 保存后的注册信息`
- `findByPluginId(String pluginId)` - `@param pluginId 插件ID` `@return 插件注册信息，不存在则返回空 Optional`
- `findAll()` - `@return 所有插件注册信息`
- `findEnabled()` - `@return 所有已启用的插件注册信息`
- `deleteByPluginId(String pluginId)` - `@param pluginId 插件ID`
</action>
  <verify>
grep -c "^ \* " scriptflow-core/src/main/java/org/team4u/scriptflow/domain/port/ScriptRepository.java && grep -c "^ \* " scriptflow-core/src/main/java/org/team4u/scriptflow/domain/port/ExecutionRepository.java && grep -c "^ \* " scriptflow-core/src/main/java/org/team4u/scriptflow/domain/port/PluginRegistryRepository.java
</verify>
  <done>所有 Repository 接口都有完整的类级别和方法 Javadoc</done>
</task>

<task type="auto">
  <name>Task 2: Add Javadoc to ScriptEngine and JsonCodec</name>
  <files>
    scriptflow-core/src/main/java/org/team4u/scriptflow/domain/port/ScriptEngine.java
    scriptflow-core/src/main/java/org/team4u/scriptflow/domain/port/JsonCodec.java
  </files>
  <action>
使用 Edit 工具为以下 2 个接口添加 Javadoc：

**1. ScriptEngine.java**:
```java
/**
 * 脚本引擎接口，定义脚本的验证和执行操作。
 * <p>
 * 该接口是核心的 Port，屏蔽了不同脚本语言引擎的差异。
 * 实现类负责脚本的语法校验和在对应的执行环境中运行脚本。
 *
 * @author jay.wu
 */
```
方法 Javadoc:
- `validate(ScriptDefinition definition)` - `@param definition 脚本定义，用于校验脚本语法` `@throws RuntimeException 当脚本语法错误时`
- `execute(ScriptDefinition definition, Map<String, Object> input, ScriptExecutionContext executionContext)` - `@param definition 脚本定义` `@param input 输入参数` `@param executionContext 执行上下文` `@return 脚本执行结果`

**2. JsonCodec.java**:
```java
/**
 * JSON 编解码接口，定义对象的序列化与反序列化操作。
 * <p>
 * 提供通用的 JSON 转换能力，支持复杂对象和集合类型的编解码。
 * 实现类可以使用 Jackson、Gson 等 JSON 处理库。
 *
 * @author jay.wu
 */
```
方法 Javadoc:
- `write(Object value)` - `@param value 要序列化的对象` `@return JSON 字符串`
- `<T> T read(String json, Class<T> type)` - `@param json JSON 字符串` `@param type 目标类型` `@return 反序列化后的对象`
- `readUntyped(String json)` - `@param json JSON 字符串` `@return 反序列化后的对象（无类型）`
- `<T> List<T> readList(String json, Class<T> elementType)` - `@param json JSON 字符串` `@param elementType 列表元素类型` `@return 反序列化后的列表`
- `readMap(String json)` - `@param json JSON 字符串` `@return 反序列化后的 Map`
</action>
  <verify>
grep -c "^ \* " scriptflow-core/src/main/java/org/team4u/scriptflow/domain/port/ScriptEngine.java && grep -c "^ \* " scriptflow-core/src/main/java/org/team4u/scriptflow/domain/port/JsonCodec.java
</verify>
  <done>ScriptEngine 和 JsonCodec 接口都有完整的类级别和方法 Javadoc</done>
</task>

</tasks>

<verification>
- grep -c "^/\\*\\*" scriptflow-core/src/main/java/org/team4u/scriptflow/domain/port/*.java 应返回 5 (5 个文件都有 Javadoc 块)
- grep "@author jay.wu" scriptflow-core/src/main/java/org/team4u/scriptflow/domain/port/*.java 应返回 5
</verification>

<success_criteria>
- [x] ScriptRepository 有完整类级别和方法 Javadoc
- [x] ExecutionRepository 有完整类级别和方法 Javadoc
- [x] PluginRegistryRepository 有完整类级别和方法 Javadoc
- [x] ScriptEngine 有完整类级别和方法 Javadoc
- [x] JsonCodec 有完整类级别和方法 Javadoc
- [x] 所有注释使用中文，包含 @author jay.wu
- [x] 接口设计意图和实现注意事项已说明
</success_criteria>

<output>
After completion, create `.planning/phases/01-domain-注释/{phase}-02-SUMMARY.md`
</output>
