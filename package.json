{
  "name": "actiondock-monorepo",
  "version": "2.0.2",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "engines": {
    "bun": ">=1.2.0"
  },
  "scripts": {
    "test": "bun test",
    "test:pack": "bun run ./scripts/pack-smoke-test.ts",
    "typecheck": "tsc --noEmit",
    "build": "bun run --filter '@actiondock/*' build",
    "docs:serve": "bunx docsify-cli serve docs"
  },
  "dependencies": {
    "@actiondock/cli": "^2.0.0",
    "@actiondock/core": "^2.0.0",
    "@actiondock/mcp": "^2.0.0",
    "@actiondock/sdk": "^2.0.0",
    "@modelcontextprotocol/server": "2.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^26.4.1",
    "execa": "^10.0.1",
    "tsx": "^4.23.13",
    "typescript": "^5.7.0"
  }
}
