services:
  action-dock:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: action-dock
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      - APP_HOME_DIR=/app/data
      - JAVA_OPTS=-Xmx512m -Xms256m
    restart: unless-stopped
