User asks: rebuild the knowledge base for a monorepo with API services, migrations, queue workers, deployment configs, and stale docs.

Expected: XL large-rebuild protocol, team_agent preferred, Plan A required, delegated gates required for all delegated stages.
