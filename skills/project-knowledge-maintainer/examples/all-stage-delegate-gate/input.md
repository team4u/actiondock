A partitioned refresh chooses execution_mode=team_agent. The Leader dispatches a workspace/noise filter delegate and a Planner delegate. The workspace/noise filter is still pending, but the Leader proceeds to create a task plan anyway. Later a Validator delegate is dispatched but slow, so the Leader writes KNOWLEDGE_UPDATE_REPORT.md with validation PASS. The ingest cleanup step also deletes .kb_inbox/api-note.md before the cleanup delegate returns.
