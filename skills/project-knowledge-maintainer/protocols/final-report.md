# Final Report Protocol

Final report may be produced only after Validator returns `PASS` or after an explicit terminal `BLOCKED` / `FAILED` state.

Include:

- scale and operation mode;
- execution mode and fallback reason;
- delegate result summary;
- files changed;
- evidence used;
- validation status;
- unresolved findings.
