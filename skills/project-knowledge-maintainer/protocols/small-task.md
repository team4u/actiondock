# Small Task Protocol

Use for one small doc area with no document-set risk.

Flow:

```text
Route-lite → Mini Plan → Apply → Validate-lite → Report
```

Escalate to M if more than one domain is involved or Worker discovery would be needed.
