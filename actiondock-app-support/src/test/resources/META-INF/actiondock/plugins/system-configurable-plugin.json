{
  "pluginId": "system-configurable-plugin",
  "name": "System Configurable Plugin",
  "description": "Test system plugin with configurable defaults.",
  "version": "0.1.0",
  "configSchema": {
    "type": "object",
    "properties": {
      "prefix": {
        "type": "string",
        "title": "Prefix"
      }
    }
  },
  "defaultConfig": {
    "prefix": "system"
  },
  "actions": [
    {
      "action": "echo",
      "title": "Echo",
      "description": "Echo with plugin config.",
      "inputSchema": {
        "type": "object"
      },
      "outputSchema": {
        "type": "object"
      }
    }
  ]
}
